services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - MONGO_URL=mongodb://mongo:27017/interbanking
      - DB_NAME=interbanking
    depends_on:
      - mongo
    volumes:
      - .:/app
    command: node server.js

  mongo:
    image: mongo:6
    volumes:
      - mongo_data:/data/db


volumes:
  mongo_data:
